"""Developer scripts (CLI helpers, one-shot maintenance jobs).

These are not part of the runtime sidecar — they ship in the source
tree so contributors and release automation can run them, but the
production binary should never import them.
"""
