# uv venv
.venv/

# Python build outputs
build/
dist/
*.egg-info/

# Bytecode
__pycache__/
*.py[cod]

# pytest
.pytest_cache/

# uv.lock is committed — pins the supply chain (paddlepaddle's ABI
# coupling to a specific paddlex/paddleocr revision matters). Devs
# share the resolved tree; CI / release builds reproduce it.
