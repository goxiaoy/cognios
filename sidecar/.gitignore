# uv venv
.venv/

# Python build outputs
build/
dist/
*.egg-info/

# Bytecode
__pycache__/
*.py[cod]

# pytest
.pytest_cache/

# uv lockfile is intentionally NOT ignored — it pins the supply chain.
# Once Phase 5 / Unit 12 lands the release packaging story we will commit
# uv.lock here. For Unit 3 (pre-Unit 12) the lockfile is regenerated on
# every uv sync; ignore it until then.
uv.lock
